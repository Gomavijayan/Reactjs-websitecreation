import React, { Component } from 'react';
export const Ex=(props)=>{
    return(
        <div>
            <h1>{props.title}</h1>
            <p>{props.body}</p>
            
        </div>
    );
}

export default Ex;